# دليل الإصدار والنشر — زيارة

> **آخر تحديث: 2026-09-21.** هذا الملف كان يصف مساراً يدويّاً بالكامل (بناء من الجهاز
> ثم Archive في Xcode) ويذكر إصدار `1.0.0+1`. الواقع تغيّر: النشر آليّ عبر Codemagic،
> والإصدار الحالي في `pubspec.yaml`. المسار اليدوي أدناه محفوظ للطوارئ لا للاستعمال المعتاد.

## 1. المسار المعتاد — آليّ

**كل دمج إلى `main` يُطلق البناء والنشر تلقائياً على المنصّتين** — عبر webhook أُنشئ في
2026-09-13 (انظر «الـwebhook» أدناه؛ قبله كان كل بناء يدوياً):

| المنصّة | الوجهة | الملف |
|---|---|---|
| iOS | TestFlight **ثم تقديم تلقائي لمراجعة App Store** | `codemagic.yaml` ← `ios-release` |
| Android | Google Play — مسار **الإنتاج** (الجمهور) | `codemagic.yaml` ← `android-release` |

**النشر للجمهور صار آلياً منذ 2026-09-21** بقرار المالك. قبل ذلك كان يقف عند قناتَي
الاختبار وينتظر ضغطة يدوية في لوحتَي المتجرين. المفتاحان هما `submit_to_app_store: true`
و`track: production` في `codemagic.yaml`، وللتراجع أعِدهما إلى `false` و`internal`.

ثلاثة آثار لهذا التبديل تستحقّ الانتباه:

1. **أندرويد فقد قناة الاختبار.** Codemagic ينشر إلى مسار واحد، فما عاد شيء يصل
   «الاختبار الداخلي». اختبار الدخان في القسم 3 صار على أندرويد عبر إصدار الإنتاج
   نفسه بعد المراجعة — أو أعِد `track: internal` مؤقّتاً حين تحتاج تجربة قبل الجمهور.
   iOS لم يفقد شيئاً: `submit_to_testflight` باقٍ على `true`، فالبناء يصل TestFlight
   ثم يُقدَّم للمراجعة.
2. **ملاحظات الإصدار.** كلا المتجرين يعرض «ما الجديد» لكل نسخة، و`codemagic.yaml` لا
   يرسل نصّاً لأيٍّ منهما. الملف `.github/whatsnew/whatsnew-ar` موجود في المستودع لكنه
   **غير موصول بشيء** (لا يذكره أيّ سير عمل). راقب أول تقديم آليّ: إن رفضه App Store
   Connect لغياب «ما الجديد»، فالعلاج إضافة مفاتيح ملاحظات الإصدار إلى كتلة
   `app_store_connect` — راجع توثيق Codemagic قبل إضافتها، فالمفتاح الخطأ يُفشل البناء
   كلّه لا خطوة النشر وحدها.
3. **كل دمج صار إصداراً عامّاً.** لا مرحلة بينهما. ما لا تريد وصوله للجمهور اليوم:
   ضع `[skip ci]` في رسالة دمج الـsquash كما في القسم التالي، أو أبقِه في طلب مفتوح.

رقم البناء يتزايد تلقائياً في Codemagic من عدّاد المتجر — لا تُعدّله في `pubspec.yaml`.
ما تُعدّله يدويّاً هو رقم الإصدار وحده (`1.2.47` في `1.2.47+246`).

**لتخطّي البناء** (تغييرات وثائق أو قواعد لا تمسّ التطبيق): أضف `[skip ci]` إلى رسالة
الالتزام. يتخطّى المنصّتين معاً.

> ⚠️ **ولا يتوقّف أثره عند Codemagic.** GitHub Actions يحترم العلامة نفسها أصلاً،
> فالالتزام الذي يحملها **يتخطّى مهامّ CI الأربع كلها**. وطلب الدمج الذي يحمله في
> رأسه يظهر **بلا فحوص** لا بفحوص خضراء — وهو أخطر، لأن غياب الفحص يُقرأ سهواً
> كنجاح. لذلك: اترك التزامات الطلب نظيفة منها، وضع العلامة في **رسالة دمج
> الـsquash** وحدها — فيُتخطّى بناء الإصدار بعد أن تكون فحوص الطلب قد جرت فعلاً.

> ⚠️ **والمطابقة نصّية بحتة — لا تعرف اقتباساً ولا نيّة.** الرسالة التي *تذكر*
> العلامة بين قوسيها، شرحاً لها أو اقتباساً في عنوان، تُتخطّى تماماً كالتي
> تقصدها. (هذه الفقرة مكتوبة لأن أوّل التزام وثّق هذا السلوك أسكتته العلامةُ
> الواردة في عنوانه هو.) فإن أردت الكتابة عنها في رسالة التزام فأسقط القوسين:
> `skip ci` عاريةً لا تطابق شيئاً. والمجموعة التي يعرفها Actions هي
> `[skip ci]` و`[ci skip]` و`[no ci]` و`[skip actions]` و`[actions skip]` —
> كلها بين قوسين معقوفين، وكلها غير حسّاسة لحالة الأحرف.

### ما يجب أن يكون مُعدّاً في Codemagic

| المجموعة | تحتوي | تخصّ |
|---|---|---|
| `appstore_credentials` | مفاتيح App Store Connect API + `MAPBOX_TOKEN` | iOS |
| `android_credentials` | `ANDROID_KEYSTORE_BASE64`، كلمات مرور المفتاح، `MAPBOX_TOKEN` | Android |
| `payment_keys` | مفاتيح ميسر وتابي وسامسونج باي القابلة للنشر | كلتاهما |
| `google_play` | `GCLOUD_SERVICE_ACCOUNT_CREDENTIALS` | Android |
| تكامل `Zyiarah Key` | مفتاح App Store Connect | iOS |

> ✅ **كل المجموعات أعلاه مُعدّة ومُتحقَّق منها.** البناء رقم ٣٠ (٣١ أغسطس ٢٠٢٦)
> أنهى خطوة Publishing بنجاح ورفع إلى مسار الاختبار الداخلي، فحساب خدمة Play
> صالح. (نسخة سابقة من هذا الملف زعمت أن `google_play` ناقصة — كان ذلك خطأً.)

### ✅ الـwebhook — أُنشئ في 2026-09-13

`triggering` في `codemagic.yaml` تطلب البناء عند الدفعة، لكن Codemagic لا يعلم بالدفعة
إلا إذا أرسلها GitHub عبر webhook على المستودع. وحتى 2026-09-13 الساعة 21:25 (+03)
**لم يكن موجوداً**: *Recent deliveries* في Codemagic كانت تقول `No deliveries yet`،
وخمس دفعات نظيفة بعد آخر بناء مسجَّل (#30 و#208 على `4f33ce9`، ٣١ أغسطس) لم تُطلق
شيئاً، والبناء #30 يقول `Started by: erihdev@gmail.com`. أي أن كل بناء قبل هذا
التاريخ بُدئ يدوياً رغم أن الكتلة مُعلَنة لـiOS منذ 2026-07-25.

أُنشئ من Codemagic ← التطبيق ← *App settings* ← **Webhooks** ← *Update webhook*،
وقبل GitHub ping التأسيس بـ`202`: «Your builds will be started on branch and tag push».

**الدليل:** أوّل دفعة بعده هي دمج هذا التعديل نفسه — بناءاه في قائمة Builds بلا
بادئ يدوي هما البرهان.

**إن توقّفت البناءات التلقائية يوماً:** افتح *Recent deliveries* هناك أولاً — الأحمر
يعني رابطاً قديماً أو خطأ استجابة، فأعد الإنشاء بـ*Update webhook*. وحتى تعود:
*Start new build* ← اختر المهمّة ← الفرع `main`.

## 2. حرّاس ما قبل الدمج

`.github/workflows/ci.yml` يشغّل خمس مهامّ على كل طلب دمج، ولا يُدمج شيء وهي حمراء:

| المهمّة | ما تفحصه |
|---|---|
| Flutter Analyze | `flutter analyze --no-fatal-infos` |
| Flutter Test | ٤٢٢ فحصاً |
| Flutter Build (full compile) | `flutter build bundle --release` — تصريف إغلاق `main.dart` كله. الوحيد الذي يكشف خطأ تصريف **داخل اعتمادية**: analyze يُبلّغ عن ملفات المشروع وحدها، وtest يصرّف ما تستورده الاختبارات فقط (درس البناء ٣١) |
| Cloud Functions Tests | lint + ٤٩ فحص وحدة + ٤٢ فحص محاكي (قواعد، أدوار، شروط الخدمة، محفظة) |
| Admin Panel Build | lint + Vitest + `tsc -b` + بناء |

## 3. اختبار الدخان بعد وصول البناء

على جهاز حقيقي، من TestFlight (iOS) أو من إصدار الإنتاج بعد المراجعة (أندرويد — لم
تعد هناك قناة داخلية، انظر القسم 1):

- **الرقم الاختباري** `+966599363888` يتخطّى الـ OTP — يلزم مراجعي أبل للدخول.
- **تسجيل الدخول** والإشعارات — مسارات أصلية (Firebase Auth/Messaging) لا يغطّيها أي
  اختبار آليّ في المستودع، فهي أوّل ما يُجرَّب بعد أي ترقية لحزم Firebase.
- **بوّابة الدفع**: أكمل عملية وتأكّد من الوصول لشاشة «الفاتورة الضريبية».
- **الخريطة**: العلامات تظهر عند إحداثيات الداير/فيفاء.
- **حذف الحساب** من الملف الشخصي يعمل — متطلّب إلزامي من أبل.

## 4. مسار الطوارئ — يدويّ

يُستعمل حين تتعطّل Codemagic فقط.

```bash
flutter clean && flutter pub get
touch .env                      # أو ضع مفاتيحك القابلة للنشر
flutter build appbundle --release   # أندرويد → build/app/outputs/bundle/release/
flutter build ipa --release         # iOS (يتطلّب macOS + Xcode)
```

التوقيع محليّاً يقرأ `android/key.properties` (مُستثنى من git). لـ iOS: افتح
`ios/Runner.xcworkspace` ثم Product ← Archive ← Distribute App.

## 5. مساران لا يصلان المتاجر

لا تستعملهما ظنّاً أنهما ينشران:

- `.github/workflows/android_release.yml` — يبني AAB على وسوم `v*` **بلا نشر**.
- `distribute_android.bat` — يبني APK ويرفعه إلى **Firebase App Distribution**
  (مختبرون داخليون)، لا إلى Google Play.
